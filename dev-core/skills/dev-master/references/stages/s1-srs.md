# 阶段 1：规格真源（不可跳过）

**技能**：`req-doc` ｜ **产出**：`dev/SRS/*.md`（`req-doc` 自身的默认落点就是这儿；存量项目命中 `docs/SRS/` 时原地续用）｜ **登记**：`SPEC_SOURCE`

## 三种入口
| 手头有什么 | 怎么走 |
|---|---|
| 已有合格 SRS | **校验 + 登记**，不重写；落在 `docs/SRS/` 的老文档**原地续用不搬家**，`SPEC_SOURCE` 记真实路径。校验按 `../../common/prd-to-srs-gate.md` §2 的三个内容角色（功能架构/页面清单/字段级详细设计） |
| 只有 PRD | `req-doc` **Step F** 转写（PRD §4/§5 → SRS 功能清单/页面清单/字段级设计）。**不要问「是否转写」，流程内默认转写** |
| 都没有 | 停下，路由 `pm-master`（要先做产品侧）或直接让用户补需求描述后跑 `req-doc` 完整生成 |
| 代码先行、文档缺失 | `req-doc` 的**反向同步模式**：从现有前端页面/接口回写 SRS |

## 铁律
阶段 2–8 的技能**全部**只认 SRS。PRD 的 §4/§5 颗粒度不够，拿它当真源的结果是下游缺字段、缺校验、缺状态机。
唯一豁免：用户**原话**说「跳过 SRS / 按 PRD 手动对齐」，且仅限单次，产出全程标注 `⚠ 降级模式`。

## 门禁（进阶段 2 前）
- [ ] `SPEC_SOURCE` 已登记进进度存档，路径真实存在
- [ ] 三个内容角色齐备（章节号按模板走，**不要死认 3.1/3.3/3.5**）
- [ ] 文档里的图由 `diagram-generator` 生成，非手绘

## 确认节点
**所有交付模式都停**。这是全流程唯一无条件确认的阶段。
